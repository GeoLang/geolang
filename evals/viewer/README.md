# Viewer eval fixtures

`snapshot.json` and `catalogue.json` are a copy of the two halves of the AG-UI
`state` the viewer sends with every chat message, so replace them with what the
viewer produces rather than editing them here. Run the eval with
`python -m evals.viewer_runner`.
