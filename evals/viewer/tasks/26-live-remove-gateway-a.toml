id = "live-remove-gateway-a"
prompt = "get rid of the Gateway A feed"
notes = "The viewer state lists no feeds, so the name comes from the prompt. Destructive, so the viewer asks first."

[expect]
action = "run"
name = "live.remove_feed"
args = { feed = "Gateway A" }
