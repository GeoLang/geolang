id = "renderer-to-the-flat-map"
prompt = "switch to the flat 2d map"
notes = "The flat 2d map is the map tab drawn by Leaflet, not a renderer.set value."

[expect]
action = "run"
name = "view.set_tab"
args = { tab = "map" }
