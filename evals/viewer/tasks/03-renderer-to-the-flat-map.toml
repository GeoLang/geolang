id = "renderer-to-the-flat-map"
prompt = "switch to the flat 2d map"
notes = "'flat 2d map' has to land on the maplibre renderer, one of the enum values."

[expect]
action = "run"
name = "renderer.set"
args = { renderer = "maplibre" }
