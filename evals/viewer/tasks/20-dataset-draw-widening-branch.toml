id = "dataset-draw-widening-branch"
prompt = "draw the widening branch of the road network"
notes = "The viewer state lists no datasets, so both names come from the prompt."

[expect]
action = "run"
name = "dataset.draw_branch"
args = { dataset = "Road Network", branch = "widening" }
